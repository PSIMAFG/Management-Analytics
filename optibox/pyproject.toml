[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "optibox"
version = "1.0.0"
description = "Asignación semanal de turnos de personal con una cadena de reglas y optimización CP-SAT."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "PySide6>=6.8,<7",
    "matplotlib>=3.9,<4",
    "openpyxl>=3.1,<4",
    "ortools>=9.12,<10",
]

[project.optional-dependencies]
dev = [
    "pytest>=8,<10",
    "ruff>=0.6,<1",
]
build = [
    "pyinstaller>=6.10,<7",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
optibox = ["data/*.sql"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"

[tool.ruff]
line-length = 120
target-version = "py311"
src = ["src", "tests"]
extend-exclude = ["build", "dist", ".venv", "./data"]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "N", "UP", "B", "SIM", "ANN", "RUF", "T20", "PTH", "C4", "RET", "PIE", "ARG"]
ignore = ["ANN401"]
allowed-confusables = ["×", "–"]

[tool.ruff.lint.per-file-ignores]
"src/optibox/ui/*" = ["N802"]
